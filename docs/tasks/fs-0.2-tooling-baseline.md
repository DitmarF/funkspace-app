# Task FS-0.2 — Reconcile tooling and run the baseline

> **Recovery pointer (FS-0.4, 2026-09-12):** this record preserves the original maintenance evidence. Later [FS-0.3 execution](fs-0.3-branch-integration-decision.md#final-execution-and-portfolio-handoff--2026-09-12) records the actual main integration, fresh CI/local validation, production and Dimi's FS-G0 approval; statements below about unpushed configuration or pending FS-G0 are historical. Current milestone scope/status is in the [feature-plan draft](../features/funkspace-minimum-usable.md). Security findings are not cleared by integration success.

## Task metadata

- **Owner/current writer:** Codex. Counterpart findings are inputs; no concurrent writer.
- **Status:** Review-ready — bounded maintenance and local baseline completed; security remediation and human acceptance remain pending.
- **Base:** `feature/wave-survivor`, `a0f22e91f2f1250ea5536973ed30598365fceffd`; initially clean target checkout at `/Users/dimi/Projects/funkspace-app`.
- **Date/environment:** 2026-09-12; macOS arm64, zsh, Node 22.22.0, pnpm 10.30.3.
- **Dependencies:** [FS-0.1 baseline and incorporated counterpart findings](fs-0.1-inventory-real-starting-point.md). Detailed EPIC 0 plan is still unavailable in the inspected local sources; Dimi promised to add it later. The current assignment explicitly authorizes bounded FS-0.2 maintenance.
- **Workflow:** [Task template](../templates/task.md), [AI workflow](../development/ai-workflow.md), root AGENTS and accepted ADRs. Only the root AGENTS applies in the target repository.

## Requested outcome and scope

Establish reproducible tooling and honest before/after results. Reconcile declarations and build order, verify frozen installation, correct feature scanning and demonstrated configuration inconsistencies, and triage the existing dependency graph without upgrading it.

Preserve application/game behavior, public contracts, token sources/generated values, thresholds and exclusions, original SVG/font assets, unrelated files, and Git refs. No commit, push, PR, deployment, settings change, history cleanup or dependency upgrade is authorized by this task. The preceding commit/push authorization applied to FS-0.1.

## Protected work and access limits

The separate project-mirror repository remains at `63d117358d67137cebff744a29fa3a9a49d82415` with three untracked combat files. It is not the target and is untouched; Dimi's retention decision is not inferred. Unsaved buffers/other devices/private assets remain unconfirmed. No uncertain area is modified.

Known generated build/test areas in the target were archived before execution to `/private/tmp/fs-0.2-evidence/generated-before.tar.gz`: Next, Storybook, game/demo output, coverage, Playwright/Lighthouse reports, Next support files and incremental typecheck output. Previous reports are not mixed with new acceptance evidence. All tracked files were fingerprinted before work. No secret values or private environment-file contents were read into evidence.

Raw command logs, per-command diffs, environment, graph snapshots and report archives live in `/private/tmp/fs-0.2-evidence/`. `runs.jsonl` records phase, revision, diff, working directory, prerequisites, exact command, versions/environment overrides, exit/outcome, evidence path, suppression limits, diagnosis and next owner/action. These are local temporary artifacts, not published CI records. The tables below preserve the durable conclusions.

## Inspection, proposal and bounded changes

1. **Version declarations:** use the already installed pnpm 10.30.3 recorded by `node_modules/.modules.yaml`, rather than CI's pnpm 9. Root `packageManager` and engines pin pnpm; CI consumes that pin. `.nvmrc` selects the verified Node 22.22.0; Node minimum 22.13.1 reuses CONTRIBUTING's existing floor and satisfies installed Style Dictionary (`>=22`) and Vite (`>=22.12` on Node 22). No runtime or package upgrade was performed; Node type declarations at 20.x are not the runtime selector.
2. **Frozen setup:** preserve `pnpm install --frozen-lockfile`. Separate Chromium installation into `pnpm setup:browsers`; remove automatic browser download from postinstall. CI and E2E now agree on `PLAYWRIGHT_BROWSERS_PATH=0`. Existing lifecycle behavior was inspected before execution. The current downloader advisory means a fresh download still requires dependency/security review.
3. **Build order:** root build generates tokens, checks common, then invokes frontend build, which builds the game before Next. CI uses this root build. Direct frontend build still requires tokens first; setup guidance says so. Storybook already regenerates tokens. No generator or generated token file was hand-edited.
4. **Tailwind:** use config-relative content paths and add `./features/**/*.{js,ts,jsx,tsx,mdx}`. A source-based probe checks actual `features/games/GameHost.tsx` and emits its `sr-only` rule using feature files alone. Before: not scanned/not emitted. After: scanned/emitted. The same class exists elsewhere, so this does not claim a previously invisible GameHost or fabricate a unique current rendering defect.
5. **Storybook:** remove the unused externally created services and unsupported provider prop in canonical preview.ts. preview.tsx delegates to the canonical file instead of carrying a second configuration. Pre-build passed despite the mismatch; this is configuration/ownership correction, not a repaired baseline compilation failure. Global typography/theme presentation verification remains FS-1.7; source typography migration remains FS-1.2.
6. **Lighthouse:** set the public flag during each build, and match Next's actual `Ready in` message. Pre-maintenance runs passed but logged readiness timeouts and retained an unresolved flag lookup in the client bundle. Keep all thresholds/runs/URLs unchanged. Both variants measure `/`, where no animated logo is mounted; the sandbox explicitly forces `enabled=true`, so it cannot independently prove default flag behavior.

README/CONTRIBUTING changes are limited to necessary runtime, setup, command and coverage guidance. No broad documentation cleanup or unrelated architecture refactor.

## Installation and command side effects

A temporary clean export of base HEAD at `/private/tmp/fs-0.2-install-check` was installed with `pnpm install --frozen-lockfile --ignore-scripts --offline`. The store contained all required packages. Its 1,092 unique installed package/version pairs match the original installed graph. Root/game manifests and bounded candidate config changes were then copied into that disposable export; a normal frozen offline install passed, followed by a complete root build without original checkout caches or build outputs.

The first temporary export helper failed because system Python did not support tarfile's `filter` argument; the resulting install reported no manifest. This was a task-helper setup failure, corrected by validating archive members before extraction. It was not a repository defect or dependency failure.

Lifecycle scripts were deliberately skipped in the first clean install. The subsequent normal frozen install and clean production build passed; a clean-machine browser download and Linux system dependency provisioning were not run. No new dependency graph was resolved. The original installed graph remains the application-check input.

Inspected side effects: token generation rewrites tracked generated files; game/Next/Storybook builds replace ignored outputs; frontend tsc updates ignored incremental metadata; coverage/E2E/LHCI write reports; Next lint runs without `--fix`; Prettier runs in check mode. Test execution sets `NEXT_TELEMETRY_DISABLED=1`, `STORYBOOK_DISABLE_TELEMETRY=1`, `PLAYWRIGHT_HTML_OPEN=never`; CI is not forced, so local Vitest's test command does not implicitly add coverage/retries. Owned local servers are stopped by their test harnesses. No existing listener was found on 3000/5173 before execution.

## Validation results

The 15 requested candidate commands were confirmed in package manifests and executed in the supplied order before maintenance. All 15 commands also passed in the same order after maintenance; independent audit/setup/probe commands are separately identified in the raw log.

| Order | Command from repository root                  | Before                                            | After | Evidence / limits                                        |
| ----- | --------------------------------------------- | ------------------------------------------------- | ----- | -------------------------------------------------------- |
| 1     | `pnpm build:tokens`                           | PASS                                              | PASS  | pre-01/post-01; inspect generated diff                   |
| 2     | `pnpm -F @funkspace/common typecheck`         | PASS                                              | PASS  | pre-02/post-02; production source only                   |
| 3     | `pnpm -F @funkspace/wave-survivor build`      | PASS                                              | PASS  | pre-03/post-03                                           |
| 4     | `pnpm -F frontend exec tsc --noEmit`          | PASS                                              | PASS  | pre-04/post-04; stories excluded by existing config      |
| 5     | `pnpm lint`                                   | PASS                                              | PASS  | pre-05/post-05; existing Next lint deprecation           |
| 6     | `pnpm test`                                   | PASS: 80 files, 1,256 tests                       | PASS  | pre-06/post-06; root jsdom includes game tests           |
| 7     | `pnpm coverage`                               | PASS                                              | PASS  | pre-07/post-07; unchanged scoped thresholds/exclusions   |
| 8     | `pnpm storybook:build`                        | PASS                                              | PASS  | pre-08/post-08; existing chunk-size warnings             |
| 9     | `pnpm build`                                  | PASS                                              | PASS  | pre-09/post-09                                           |
| 10    | `pnpm e2e`                                    | PASS: 7 tests                                     | PASS  | pre-10/post-10; Desktop Chromium only, filtered contrast |
| 11    | `pnpm lhci:off`                               | PASS with readiness warning; flag not established | PASS  | pre-11/post-11; three desktop runs of `/`                |
| 12    | `pnpm lhci:on`                                | PASS with readiness warning; flag not established | PASS  | pre-12/post-12; three desktop runs of `/`                |
| 13    | `pnpm -F @funkspace/wave-survivor typecheck`  | PASS                                              | PASS  | pre-13/post-13; test files excluded                      |
| 14    | `pnpm -F @funkspace/wave-survivor test`       | PASS: 58 files, 920 tests                         | PASS  | pre-14/post-14; dedicated Node environment               |
| 15    | `pnpm -F @funkspace/wave-survivor demo:build` | PASS                                              | PASS  | pre-15/post-15                                           |

Additional standalone demo E2E, before and after maintenance: `pnpm exec cross-env PLAYWRIGHT_BROWSERS_PATH=0 playwright test --config playwright.demo.config.ts` passed all 18 tests in both runs. It uses its own Vite server and Chromium configuration, including synthetic viewport/reduced-motion cases. WebKit, real phone interaction, live CI, deployment, and portfolio visual approval remain NOT RUN.

### Exact contrast suppression and coverage limits

- `e2e/home.a11y.spec.ts`, lines 83–105: only `color-contrast` violations are transformed. Nodes with numeric contrastRatio >=2.4 are removed; ratios below 2.4 and missing/non-numeric ratios remain. Violations with no remaining nodes are dropped. Other violation kinds remain.
- `e2e/logo-animation.spec.ts`, lines 183–202: a whole color-contrast violation is retained only if some node has a numeric ratio below 2.4. A missing/non-numeric ratio alone does not retain that violation. Other violation kinds remain. This additional suppression is now recorded alongside the parent-plan report.
- Both suppressions are unchanged. A filtered pass proves neither WCAG AA contrast nor full accessibility. Actual source-color repair and removal of both filters belong together to FS-1.2.
- Root coverage uses global thresholds of 75% lines/functions/statements and 80% branches, with existing exclusions for application/infrastructure/domain, routes/layouts, stories and selected utilities/components/hooks. Dedicated game execution is not equivalent to game coverage. Production common/game typechecks omit tests.

## Dependency/security review

No audit-fix, overrides, dependency upgrades or lockfile changes were applied. Full workspace `pnpm audit --json` returned exit 1: 130 reported vulnerability instances (6 critical, 74 high, 37 moderate, 13 low), represented by 118 advisory/range records across 44 package names; 1,093 dependencies in registry summary. Production-only audit returned exit 1: 16 instances (2 critical, 10 high, 3 moderate, 1 low), 99 dependencies. Counts are audit metadata, not unique exploitable application defects; audit's devDependencies=0 field is not evidence that this is all production code.

The full locked graph was audited; `pnpm list -r --depth Infinity --json` and the isolated frozen install independently identify the installed graph. Registry requests verified publication of all 103 distinct candidate package/version pairs derived from patched bounds and required companion proposals. These metadata queries did not resolve/install a candidate graph or prove compatibility. Raw reports include dependency paths and exact advisory URLs. See [security-matrix.json](/private/tmp/fs-0.2-evidence/security-matrix.json) and its [readable appendix](/private/tmp/fs-0.2-evidence/security-matrix.md) for all 118 advisory/range records, paths, exact per-advisory candidates, relevance, lockfile impact, checks, ownership and disposition. A per-advisory minimum is not a coherent combined upgrade proposal.

### Owned dependency proposals — not applied

| ID / priority                     | Path and relevance                                                                                                                                                                                                                            | Exact proposal / lockfile implications                                                                                                                                                                                                                                                                                   | Required review/checks and latest-needed owner action                                                                                                                                                                                                                                                                                                                                   |
| --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FS02-D01 / production             | frontend → next 15.5.23. Two critical advisories concern Windows hosting and AVIF optimization. This run is macOS; no next/image imports or remote image patterns were found, but deployment OS and image endpoint exposure were not audited. | Review Next 15.5.24 with eslint-config-next and @next/eslint-plugin-next 15.5.24 at all existing declarations. These exact releases are published. Expect manifest/Next/peer snapshot changes; this does not resolve every transitive advisory: 15.5.24 still pins postcss 8.4.31.                                       | Codex proposes a bounded patch; Dimi approves upgrade scope before release acceptance. Frozen install, types/lint, builds, frontend/game E2E, and production/full re-audit. [Publisher Windows advisory](https://github.com/vercel/next.js/security/advisories/GHSA-p293-qw3h-jr36), [AVIF advisory](https://github.com/vercel/next.js/security/advisories/GHSA-2xp9-vwfh-vxw4).        |
| FS02-D02 / setup                  | root → @playwright/test → playwright 1.53.2; also appears through Next's peer graph. Existing local Chromium was used.                                                                                                                        | Review @playwright/test 1.55.1, which brings the downloader fix. Browser revision/lock snapshots will change; coordinate browser provisioning rather than silently downloading with the current vulnerable downloader.                                                                                                   | Codex/Dimi before any fresh browser download or new CI environment. Verify certificate-safe setup, both E2E configurations and re-audit. [Advisory](https://github.com/advisories/GHSA-7mvr-c777-76hp).                                                                                                                                                                                 |
| FS02-D03 / Storybook              | frontend → storybook 10.0.6. No .env file was located in the searched checkout; no publishing occurred. Local static smoke testing does not remove build/publish or development-WebSocket risks.                                              | Review storybook 10.2.10 and align the existing Storybook package family after peer review. Core release publication is verified; companion graph not resolved.                                                                                                                                                          | Codex/Sites and Dimi before publishing Storybook or exposing its development server. Build, provider/logo/button smoke, theme/font checks, peer compatibility and re-audit. [Build advisory](https://github.com/storybookjs/storybook/security/advisories/GHSA-8452-54wp-rmv6), [WebSocket advisory](https://github.com/storybookjs/storybook/security/advisories/GHSA-mjf5-7g4m-gx5w). |
| FS02-D04 / test tooling           | root/game → vitest 3.2.4 and root → coverage-v8 → browser 3.2.4. Current suites use run mode with jsdom/Node; no UI/browser server or standalone mocker plugin is configured.                                                                 | A complete current-advisory proposal requires separate review of Vitest/coverage 4.1.11 and its browser/mocker graph. This is a major upgrade, not FS-0.2 maintenance. Registry/GitHub Reviewed metadata list 3.2.6 for one older fix while publisher text still lists 3.2.5; neither clears the newer <4.1.11 advisory. | Codex prepares compatibility/coverage migration proposal; Dimi approves scope before exposed UI/browser use. Preserve thresholds and test discovery, rerun both suites/coverage/build/E2E. [Current publisher advisory](https://github.com/vitest-dev/vitest/security/advisories/GHSA-82fw-gwwq-j7x9), [reviewed older advisory](https://github.com/advisories/GHSA-5xrq-8626-4rwp).    |
| FS02-D05 / generators and servers | game/Storybook/Vitest → vite 7.0.2; root → style-dictionary 5.1.1. Local tracked inputs and loopback checks only; advisory preconditions vary.                                                                                                | Review Vite 7.3.5 and Style Dictionary 5.4.4 in separate bounded proposals. Published versions verified. Any changed snapshots/peers/generated output need inspection.                                                                                                                                                   | Codex before untrusted input/exposed development-server use; Dimi authorizes upgrades. Token generation/diff and game-theme regression for Style Dictionary; Storybook/demo/E2E for Vite; re-audit. Full advisory paths/candidates in matrix.                                                                                                                                           |
| FS02-D06 / transitive remainder   | Includes Next → sharp/postcss/nanoid/build tooling and LHCI → proxy-agent → pac-proxy-agent → get-uri → basic-ftp. Filesystem LHCI upload is not an application FTP feature.                                                                  | Per-advisory candidates and verified publication are in the matrix, including sharp 0.35.4 and basic-ftp 5.3.1. Parent constraints and native compatibility are unresolved; no forced overrides. image-size and extract-zip advisories report no patched version.                                                        | Codex triages parents and runtime reachability before release/untrusted processing; Dimi approves bounded update/replacement scope. Exact combined lock diff and candidate checks remain NOT RUN.                                                                                                                                                                                       |

No clean security report or upgrade approval is claimed. These are owned unresolved findings, not introduced regressions or reasons to weaken checks.

### Authoritative tooling references

Inspected 2026-09-12: [pnpm frozen installation](https://pnpm.io/10.x/cli/install), [pnpm action-setup packageManager input](https://github.com/pnpm/action-setup/tree/v4), [Tailwind v3 config-relative scanning](https://v3.tailwindcss.com/docs/content-configuration#using-relative-paths), and [Next build-time public environment variables](https://nextjs.org/docs/app/guides/environment-variables). Installed package manifests establish the runtime/tool engine requirements; registry metadata establishes candidate publication, not upgrade compatibility.

## Completion evidence and intentional diff

- All 15 before/after command pairs returned exit 0. Root test/coverage: 80 files, 1,256 tests; dedicated game: 58 files, 920 tests. Counts overlap and must not be added as independent coverage. Coverage stayed at 91.19% statements/lines, 87.98% branches and 83.33% functions within the unchanged scope.
- Default E2E: 7/7; standalone demo: 18/18 before and after. Built Storybook Button/Primary and LogoMotion/FeatureFlagOff rendered in an ephemeral loopback server with no page errors; no visual approval claimed.
- The actual Tailwind CLI emitted identical CSS from root and frontend working directories, including sr-only. Existing Browserslist/baseline-data age warnings were recorded; their suggested automatic latest-version updates were not run.
- Post Lighthouse: three OFF LCP samples 608.29/645.24/644.19ms; three ON samples 647.71/604.65/645.29ms; CLS 0 and performance score 1 in all six. Existing LCP 2500ms, CLS 0.1, three-run desktop settings and p75 assertion configuration remain unchanged. These local samples do not establish field p75.
- Saved compiled bundles verify the default flag expression is false in OFF (`void 0!==o&&o`) and true in ON (`void 0===o||o`); explicit true/false overrides still work and neither bundle contains an unresolved public-flag lookup. Report manifests identify only the three fresh runs per variant. This proves different configured builds, not a measured animation cost on the current home route.
- Token source and generated CSS/TS fingerprints and the root/scratch lockfile remain unchanged. Separate-copy protected files and all application/game implementation files remain unchanged. Initial formatting issues in the edited preview/guidance were corrected before the post-maintenance lint pass; no tests, thresholds or exclusions changed.
- Candidate: base SHA above plus the local uncommitted diff. Raw logs/phase diffs are under the evidence directory; the final [candidate patch](/private/tmp/fs-0.2-evidence/final.diff) includes the untracked task record. A [compact evidence archive](/private/tmp/fs-0.2-evidence.tar.gz) preserves logs, metadata, probes and final diff; older generated outputs remain separately archived locally. Retain these local artifacts with acceptance if durable off-machine evidence is needed.

Actual changed files:

| File                                                                    | Change                                                                          |
| ----------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| `.nvmrc`                                                                | Verified Node baseline pin                                                      |
| `package.json`                                                          | pnpm/runtime declarations; explicit browser setup; root build and LHCI ordering |
| `games/wave-survivor/package.json`                                      | Existing documented Node minimum; no game dependency/script/contract changes    |
| `.github/workflows/ci.yml`                                              | Read shared Node/pnpm pins; consistent browser path; root build                 |
| `.lighthouse/lighthouserc.off.json`, `.lighthouse/lighthouserc.on.json` | Actual readiness message only; incidental existing JSON whitespace normalized   |
| `frontend/tailwind.config.ts`                                           | Config-relative feature scanning                                                |
| `frontend/.storybook/preview.ts`, `preview.tsx`                         | One canonical provider/configuration path                                       |
| `README.md`, `CONTRIBUTING.md`                                          | Necessary setup/version/command and coverage guidance                           |
| `docs/tasks/fs-0.1-inventory-real-starting-point.md`                    | Dated FS-0.2 follow-up; original inventory evidence retained                    |
| `docs/tasks/fs-0.2-tooling-baseline.md`                                 | This substantial task record and evidence handoff                               |

Runtime/public game contracts are unchanged. Setup contract changes are explicit: installation no longer downloads Chromium automatically, root build regenerates tokens, and Lighthouse flags are supplied at build time.

## Remaining work and handoff

Codex completed the local maintenance baseline and incorporated a dated FS-0.2 follow-up into the FS-0.1 record. Live GitHub Actions/Linux provisioning and WebKit/real-device checks remain NOT RUN; the configuration has not been pushed. Sites should use the pinned environment and root command sequence above; typography/token repairs, logo static behavior/pathCount, storage-denied theme consistency and future UI contracts retain their FS-0.1 owners/tasks.

Dimi supplies the detailed EPIC 0 local plan and preservation confirmation when ready. Any dependency upgrade requires a reviewed, explicit scope; FS-G0 and all human acceptance remain pending. This maintenance task does not retrospectively approve FS-0.1 gates.

**Dimi's next acceptance action:** review this maintenance diff and the owned security proposals, provide the outstanding detailed plan/preservation information, and decide the scope of any subsequent dependency update. No routine maintenance confirmation is needed to keep this candidate reviewable; no human gate has been marked approved.

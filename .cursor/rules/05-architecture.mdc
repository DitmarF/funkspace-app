---
description: "FunkSpace — Clean Architecture & Clean Code"
globs: ["frontend/domain/**/*.ts", "frontend/application/**/*.ts", "frontend/infrastructure/**/*.ts", "frontend/components/**/*.tsx", "frontend/hooks/**/*.ts"]
alwaysApply: false
---

## Architecture Layers

FunkSpace follows Clean Architecture with four main layers:

1. **Domain** (`frontend/domain/`) - Pure business logic, entities, and rules
2. **Application** (`frontend/application/`) - Use cases and orchestration
3. **Infrastructure** (`frontend/infrastructure/`) - External concerns (DOM, storage, browser APIs)
4. **Presentation** (`frontend/components/`, `frontend/hooks/`, `frontend/app/`) - UI components and React hooks

## Dependency Rules

Dependencies point **inward**:

```
Presentation → Application → Domain ← Infrastructure
```

### Import Boundaries

- **Domain**: No dependencies (pure TypeScript)
- **Application**: Imports Domain only
- **Infrastructure**: Implements Domain ports/interfaces (depends on Domain)
- **Presentation**: Uses Application services via hooks (depends on Application and Domain types)

### Valid Imports

✅ **Application importing from Domain:**
```typescript
import type { Theme } from "@/domain/theme/Theme";
```

✅ **Infrastructure implementing Domain port:**
```typescript
import type { StoragePort } from "@/domain/ports/StoragePort";
export class LocalStorageAdapter implements StoragePort { ... }
```

✅ **Presentation using Application service via hook:**
```typescript
import { useTheme } from "@/hooks/useTheme";
```

### Invalid Imports

❌ **Domain importing from Application/Infrastructure/Presentation:**
```typescript
// WRONG
import { ThemeService } from "@/application/theme/ThemeService";
```

❌ **Application importing from Infrastructure:**
```typescript
// WRONG
import { LocalStorageAdapter } from "@/infrastructure/storage/LocalStorageAdapter";
```

❌ **Presentation directly importing from Infrastructure/Application:**
```typescript
// WRONG - use hooks instead
import { ThemeService } from "@/application/theme/ThemeService";
```

## Service Injection Pattern

Services are injected via React Context:

1. **Infrastructure creates services**: `createServices()` in `infrastructure/services/`
2. **Application provides via Context**: `<ServiceProvider services={services}>`
3. **Presentation consumes via hooks**: `const { themeService } = useServices()`

Components should use hooks (e.g., `useTheme`, `useScrollProgressService`) to access services, not import services directly.

## File Organization

- `domain/` - Entities, business rules, port interfaces
- `application/` - Services, orchestrators, service providers
- `infrastructure/` - Adapters, utilities, service factory
- `components/` - React components (presentation layer)
- `hooks/` - React hooks (thin wrappers around services)
- `app/` - Next.js App Router pages (presentation layer)

## Clean Code & Design Principles

### Single Responsibility & Idempotency

- Give each variable, type, function, and module **one clear reason to change**.
  - Variables: represent a **single concept**; avoid reusing the same variable for multiple meanings.
  - Types/interfaces: describe one cohesive shape or role; split if the type grows unrelated fields.
  - Functions: do **one thing** at one level of abstraction; prefer small, composable helpers.
  - Modules/files: group closely related behavior; extract when a file becomes a “grab‑bag”.
- Aim for **idempotent** functions where practical:
  - Pure functions (no hidden state, no I/O) are easiest to test and reuse.
  - Application and infrastructure services should minimize hidden side effects and make them explicit via return types or events.

### Modular Architecture Design

- Map modules to layers:
  - Domain modules express **business concepts** (e.g., animations, theme) without framework imports.
  - Application modules orchestrate **use cases** using domain entities and ports.
  - Infrastructure modules adapt to **browser/DOM/storage** and external APIs.
  - Presentation modules render **UI**, delegating behavior to hooks/services.
- When to introduce a new module:
  - The concept is reused across multiple features.
  - A file exceeds a small, understandable scope (e.g., > ~200–250 lines or multiple unrelated concerns).
  - You need to swap implementations (e.g., local storage vs. in‑memory) behind a port.

### Function & Object Design

- Prefer:
  - Pure functions for domain logic.
  - Small, named helpers instead of long inline callbacks.
  - Data‑first design: pass explicit parameters, avoid grabbing global state.
- Avoid:
  - Long parameter lists without grouping (introduce small value objects where helpful).
  - Deep object mutation across layers; prefer returning new values.

## Testing Strategy

- **Unit Tests**: Test domain logic and application services in isolation
- **Integration Tests**: Test service interactions with infrastructure adapters
- **Component Tests**: Test presentation components with mocked services
- **E2E Tests**: Test full user flows

Tests should be colocated with source files (`*.test.tsx` next to source).

## Reference

See `docs/architecture.md` for detailed architecture documentation, migration notes, and best practices.

# Task FS-0.3 — Prepare the branch integration decision

## Task metadata

- **Owner/current writer:** Codex.
- **Status:** In progress — Dimi selected A, accepted the portfolio branch name and approved FS-G0 on 2026-09-12. Integration review/validation is underway; no resulting main revision is claimed yet.
- **Inspected candidate:** `7c70984f48d05a8d41baea24384337f58dd2053d` on `feature/wave-survivor`.
- **Environment:** 2026-09-12; local `/Users/dimi/Projects/funkspace-app`, macOS arm64; Node 22.22.0, pnpm 10.30.3, Git 2.50.1, gh 2.86.0. Target initially clean, including index and untracked files.
- **Dependencies:** [FS-0.1 inventory/counterpart findings](fs-0.1-inventory-real-starting-point.md), [FS-0.2 baseline](fs-0.2-tooling-baseline.md), its accessible logs and exact candidate patch.
- **Workflow/context:** [Task template](../templates/task.md), [AI workflow](../development/ai-workflow.md), root [AGENTS](../../AGENTS.md), ADRs [001](../decisions/ADR-001-monorepo-strategy.md), [002](../decisions/ADR-002-design-token-source-of-truth.md), [003](../decisions/ADR-003-interactive-experience-boundary.md), [004](../decisions/ADR-004-game-development-architecture.md), [game acceptance](../features/wave-survivor-implementation-plan.md#gate-2-decision).
- **Source-plan limit:** `/Users/dimi/Downloads/FunkSpace_Minimum_Usable_Development_Plan.md` is accessible. The detailed EPIC 0 plan remains unavailable in inspected local sources; Dimi said it will be added later. The current assignment supplies the decision scope. Documents are context, not authorization to execute their commands.

## Dimi decision and integration continuation — 2026-09-12

This section supersedes the pending-decision statements in the original inspection record below. Dimi explicitly requested: “review and integrate the accepted game baseline into `main`, validate the resulting revision, then start portfolio work from it”; accepted `feature/funkspace-minimum-usable`; and stated “FS-G0 is approved”. Record **A selected**, **branch name accepted**, and **FS-G0 approved by Dimi**. The fixed unmerged base in option B is not selected. Approval does not invent passing integration checks, waive unresolved defects, or identify previously undisclosed local work.

Reinspection starts from clean local/live `feature/wave-survivor` at `9d81195153e85f3093b5279990b8d4de11764c07`; local/live main remains `15c0f1c43a976fb345dbf642ceb7445cf70fcabd`. The newer game-branch tip adds only this decision document to the FS-0.2 candidate `7c70984f48d05a8d41baea24384337f58dd2053d`. Main is still an ancestor. The proposed squash integration retains the game branch, docs refs, accepted gameplay, public APIs, tokens, assets and the separate clone's three untracked combat files. No branch deletion, dependency upgrade or settings change is requested.

Read-only Vercel dashboard inspection is now accessible and refines FS03-03:

- Project `funkspace-app-frontend`, production branch `main`, automatic custom-domain assignment enabled, production domain `funkspace.de`. A main merge will create a production deployment and automatically assign its custom domains on success. No deployment checks are configured in Vercel. This is a public-release consequence, not merely a Git operation; obtain explicit approval for that consequence before the final merge.
- Root Directory is `frontend`; outside-root files are included; build override is `pnpm run build`; install override is disabled; Node selector is 22.x. The provider therefore invokes the frontend build script, bypassing root token generation/common typecheck. Existing committed token outputs are retained, but a successful provider build does not prove the documented root build order. Proposed separate setting correction: build command `pnpm --workspace-root build`, install command `pnpm install --frozen-lockfile`; preserve root directory and outside-root access. No setting was changed.
- GitHub still permits squash/rebase/merge commits, disables automatic branch deletion, and has no required checks/reviews in current main protection. Use reviewed input and fresh CI voluntarily; do not restore the stale protection snapshot or bypass failing checks.

The [Playwright advisory](https://github.com/advisories/GHSA-7mvr-c777-76hp) specifically identifies macOS branded Chrome/Edge shell installers using `curl -k`. Installed `playwright-core@1.53.2` maps bundled `chromium` to its archive downloader rather than those scripts. The downloader uses Node HTTPS certificate verification by default; its HTTPS-proxy branch explicitly disables certificate verification. The current Ubuntu CI requests bundled Chromium, and the workflow supplies no proxy/TLS override. That narrows the original provisioning concern: do not claim every Chromium installation invokes the cited macOS flaw. Do not use insecure proxy/TLS overrides or branded macOS installers. The dependency advisory remains owned; no upgrade or clean-security claim follows from this path inspection.

| Item                                                                | Approved status                          | Executed / validated status                                    |
| ------------------------------------------------------------------- | ---------------------------------------- | -------------------------------------------------------------- |
| Strategy A and resulting-main base rule                             | Approved by Dimi, 2026-09-12             | Review preparation underway; actual resulting main SHA pending |
| `feature/funkspace-minimum-usable`                                  | Name accepted by Dimi                    | Creation follows successful resulting-main validation          |
| FS-G0                                                               | Approved explicitly by Dimi, 2026-09-12  | Human approval recorded separately from integration evidence   |
| Production release caused by merge                                  | Exact automatic consequence now verified | Await explicit approval before final merge                     |
| Vercel/GitHub setting changes; dependency upgrades; branch deletion | Not approved by this decision            | Not performed                                                  |

Continuation evidence is stored separately in `/private/tmp/fs-0.3-integration/`; original inspection artifacts remain unchanged. Complete the review, prepare a concrete PR and collect fresh checks before requesting the final public-release approval. Portfolio implementation is not part of the branch integration operation; hand off the validated base for the next assigned milestone task.

### Reviewed candidate and fresh validation

[PR 274 — Integrate accepted Wave Survivor baseline](https://github.com/DitmarF/funkspace-app/pull/274) is open. Exact head: `4bc3e2ce6d9cedd1942172ac3b7198f4b8eb911c`; exact base: `15c0f1c43a976fb345dbf642ceb7445cf70fcabd`. The head adds the recorded Dimi decision; all executable/configuration files still match the FS-0.2 candidate. GitHub reports `MERGEABLE` / `CLEAN`, 163 changed paths, 26,233 additions and 292 deletions. No main merge has occurred and no portfolio branch has been created.

Fresh [CI run 34693931569](https://github.com/DitmarF/funkspace-app/actions/runs/34693931569) completed **PASS**. Its checkout log identifies synthetic PR merge `bc5d6a4163fc808ea1acffdc4d3a3c5e918e4f25`, combining the exact head/base above. This is PR integration evidence, not an actual resulting-main SHA or post-merge validation. Node 22.22.0 and pnpm 10.30.3 were used on Ubuntu. Frozen installation, token generation, lint/format, frontend types, 1,256 tests/80 files with coverage, bundled Chromium installation, 7 default E2E tests, root production build and LHCI OFF/ON all passed. The default browser and contrast/coverage exclusions remain unchanged. CodeQL and Vercel checks also report success.

Six complementary local commands ran at exact head `4bc3e2ce6d9cedd1942172ac3b7198f4b8eb911c` on macOS arm64, Node 22.22.0/pnpm 10.30.3, with the existing frozen graph and installed Chromium. All exited zero:

| Command                                                                                                                                                         | Result                                                      |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| `pnpm -F @funkspace/wave-survivor typecheck`                                                                                                                    | PASS                                                        |
| `pnpm -F @funkspace/wave-survivor test`                                                                                                                         | PASS: 920 tests / 58 files; overlaps CI coverage            |
| `pnpm -F @funkspace/wave-survivor demo:build`                                                                                                                   | PASS                                                        |
| `pnpm exec eslint --config frontend/eslint.config.mjs games/wave-survivor/src games/wave-survivor/demo/main.js e2e/wave-survivor/demo.spec.ts --max-warnings=0` | PASS; existing React/pages discovery notices retained       |
| `pnpm exec cross-env PLAYWRIGHT_BROWSERS_PATH=0 playwright test --config playwright.demo.config.ts`                                                             | PASS: 18 Chromium tests; no WebKit or physical-device claim |
| `pnpm storybook:build`                                                                                                                                          | PASS; existing large-chunk warning retained                 |

The [Vercel preview](https://vercel.com/ditmarfs-projects/funkspace-app-frontend/66J2HteHrF6Vr6ZenspHyMf3DGqK) is Ready at the same exact head. Its logs confirm pnpm 10.30.3, Next 15.5.23, restored cache, and `pnpm run build` in `/vercel/path0/frontend`; they do not demonstrate a clean root build or frozen-install enforcement. Root CI provides separate fresh build evidence. The provider reports ignored dependency lifecycle scripts; do not blanket-approve them. Main still serves the previous production revision; no production deployment was initiated in this continuation.

Review reconfirmed the inherited contract differences from main: game mount options add `viewport`, status and discrete event callbacks; controllers add `chooseUpgrade`; package exports include events, upgrade options, run results and runtime phase. `GameHost` removes width/height props and delegates sizing to the game. Host/loader tests cover the accepted compatibility surface. No portfolio route mounts the host; upgrade/result portfolio UI remains later work. No frontend route/component, token source/generated file, asset or lockfile differs from main as part of the integration input. All tracked implementation fingerprints and the protected separate clone remain unchanged during this continuation.

The approval/strategy record was committed and pushed as `4bc3e2ce6d9cedd1942172ac3b7198f4b8eb911c`. This validation addendum is a separate uncommitted documentation diff; it is not part of the tested PR head. Before a later merge, preserve/incorporate it deliberately and obtain fresh CI if the PR head changes. Never reuse this run for a different head or claim it validates the eventual squash commit.

**Remaining action-specific approval:** correct Vercel's build override to `pnpm --workspace-root build` and install override to `pnpm install --frozen-lockfile`, validate a new preview using those settings, then squash-merge PR 274 with its automatic production publication to `funkspace.de`. The settings and public-release consequence require approval under Dimi's session rules; strategy/name and FS-G0 approval are already recorded and are not being requested again. GitHub protection changes, upgrades and branch deletion remain excluded. After an approved merge, capture the actual main SHA, run the full resulting-revision validation, and only then create `feature/funkspace-minimum-usable` from it.

The [continuation evidence archive](/private/tmp/fs-0.3-integration-evidence.tar.gz) retains command records/logs, exact PR/CI metadata, provider observations, reviewed input diff and the [documentation addendum](/private/tmp/fs-0.3-integration/documentation.diff). Application/CI pre-integration checks above are PASS; actual merge, resulting-main validation and portfolio branch creation are **NOT RUN**, pending the explicit production/settings approval. FS-G0 remains **approved by Dimi**, separately from those execution statuses.

## Original inspection record — before Dimi's decision

The remainder preserves the original 2026-09-12 proposal and its evidence. Its pending statuses are historical and superseded by the continuation above.

## Requested outcome, scope and acceptance

Prepare two concrete integration options and a recommendation grounded in the actual validated game candidate, retained work, remote policy, CI and deployment evidence. This task changes documentation only. No runtime/public contract, configuration, dependency, generated file, branch, index or remote state is changed.

- [x] Recheck local and remote inputs against FS-0.2, preserving qualified validation provenance.
- [x] Compare ancestry, retained work, integration requirements and accessible CI/deployment evidence.
- [x] Document options A and B, exact inputs, approvals, base-selection rules and deferred actions.
- [x] Keep proposed, approved, executed and validated states separate; provide a continuation handoff.
- [ ] Dimi selects and approves a precise strategy/base rule. Recommendation is not approval.
- [ ] Any later authorized integration is validated at its actual resulting revision; not applicable yet because none was executed.

The implementation sequence is inspection, one task record, then path/reference/format/diff and preservation validation. No additional planning system or ADR is needed: the architecture remains unchanged.

## Current identity, ancestry and retained work

Live GitHub GET responses agree with the local/cached main and game refs at inspection time. No fetch, prune, checkout, merge simulation that writes objects, or branch mutation was performed.

| Input                                                                                                     | Exact revision                             | Verified relationship and disposition                                                                                                       |
| --------------------------------------------------------------------------------------------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------- |
| Local `main`, cached `origin/main`, live GitHub `main`                                                    | `15c0f1c43a976fb345dbf642ceb7445cf70fcabd` | Merge base of main and game; main has zero unique commits relative to the game candidate. Retain.                                           |
| Local `feature/wave-survivor`, its configured upstream `origin/feature/wave-survivor`, live GitHub branch | `7c70984f48d05a8d41baea24384337f58dd2053d` | 81 commits ahead / 0 behind main; all 81 are `+` in `git cherry main HEAD`. Retain full game, tooling, documentation and provenance.        |
| FS-0.2 parent                                                                                             | `a0f22e91f2f1250ea5536973ed30598365fceffd` | FS-0.1 inventory/counterpart record; exact parent of the candidate.                                                                         |
| Historical supplied inspection reference                                                                  | `b8128d5220a09a6f79117ace388d7ae1b54b7d68` | Ancestor two commits behind the current tip. Includes bounded lifecycle callback cleanup and recorded Gate 2 approval; not the current tip. |
| Local/cached `docs/expand-agent-guide`                                                                    | `b5eec35e93c50d20abfd13b8451862e79a0ad587` | Ancestor of main, 0 ahead / 23 behind main. Absent from live branch listing; cached remote ref is stale. No deletion proposed.              |

The main-to-candidate diff contains **162 paths, 26,076 insertions and 292 deletions**. It retains the completed standalone game, deterministic simulation/input/rendering and tests, demo Start/upgrade/result/replay flow, host/loader compatibility changes, separate demo E2E configuration, game documentation, and FS-0.1/0.2 maintenance. Full paths and commits are in the evidence artifacts below. The lockfile and source/generated tokens have no main-to-candidate change. Existing frontend component implementations and assets remain retained; portfolio routes do not yet render `GameHost`.

At these exact inputs there are no divergent mainline commits to reconcile; ancestry permits a fast-forward tree transition. This is not a GitHub PR mergeability result or a guarantee about future conflicts. No integration was simulated or executed. Refresh both tips before any authorized continuation, particularly if a dependency PR lands first.

The docs branch has explicit ancestry through merge `bb2d9dad27c190184af2df037055bb1bc6800e0d` (parents `dfafa629c9dff46d6dd5684864ab2fc9e13b910a` and the docs tip). [PR 251](https://github.com/DitmarF/funkspace-app/pull/251) separately reports head `dca9d95307257e811e9396d891694adad075d07c` and merged commit `dfafa629c9dff46d6dd5684864ab2fc9e13b910a`, whose single parent is consistent with squash integration. These are distinct evidence types. Patch equivalence does not prove semantic retention after squash/cherry-pick, and missing ancestry alone would not justify deletion. FS-0.1's retained-document review remains applicable; no cleanup is required here.

Live GitHub lists 22 open Dependabot PRs (252–273), all targeting the inspected main, and no game integration PR. Their heads, titles and base SHAs are captured. They are not part of either proposed input and have not been accepted, merged or independently validated by this task.

### Protected work and access limits

The separate project-mirror clone at `/Users/dimi/.codex/.chatgpt-projects/g-p-6a8710cd7ee88191854df38f76499083/funkspace-app` remains at `63d117358d67137cebff744a29fa3a9a49d82415`, with three untracked `games/wave-survivor/src/domain/combat/` files: `BasicAttackDefinition.ts`, `BasicAttackDefinition.test.ts` and `index.ts`. It is not the candidate. Preserve it independently; do not copy, stash, reset or clean it. Dimi's retention/ownership confirmation for that work and undisclosed assets, unsaved buffers or other devices remains pending. The target's clean state does not resolve those questions.

Read-only GitHub access includes branch protection, repository merge settings, rules, checks, runs, PRs and deployment metadata. No Vercel settings connector or local `.vercel/project.json` was available. Vercel dashboard configuration, build logs, production alias mapping and secret/environment values were not inspected. No secret values are included in evidence.

## Exact validation and game acceptance provenance

The FS-0.2 task record describes its original precommit worktree; its statements that the configuration was unpushed are historical. The current commit and live remote now establish the pushed candidate above. FS-0.3 reconstructed the complete parent-to-candidate patch, including the new task record, and found it **byte-identical** to `/private/tmp/fs-0.2-evidence/final.diff`. Each of the 15 recorded post-maintenance command diffs plus the standalone-demo E2E diff matches the final executable/configuration changes. All 16 log files exist and their recorded exits are zero. This maps the local validation to the committed candidate without pretending the commands ran after that commit existed.

FS-0.2 recorded all 15 requested commands before and after maintenance, plus separate demo E2E. Post results: root test/coverage 1,256 tests in 80 files; dedicated game 920 in 58 files (overlapping coverage); default E2E 7 Chromium tests and demo E2E 18. Token/common/game/frontend checks, lint, Storybook, production and demo builds, both LHCI variants and the separate clean frozen offline installation/build passed within their documented limits. No application checks were rerun in FS-0.3.

Game Gate 2 approval is recorded from Dimi on 2026-09-06 for `e439a13d10e10f2f4447bdf9fdbc4aee4ffa2e5f`, with gameplay from `b8965eef26e84a175c894f4f4745003a7a41de73`. The later callback guards/tests in `b8128d5220a09a6f79117ace388d7ae1b54b7d68` have documented regression evidence; subsequent FS-0.1/0.2 commits change no game implementation or gameplay tuning. Preserve four normal waves plus boss, accepted pacing and the public game boundary. Human duration measurements and detailed device/browser models were not recorded. No new human approval or portfolio visual acceptance is inferred.

Validation limits carry forward: default E2E excludes the demo; both browser configurations use Chromium, not WebKit/real devices. Coverage exclusions remain. Existing 2.4:1 contrast filters are scoped differently in home and logo E2E and do not establish full contrast correctness; source repair and filter removal stay together in FS-1.2. LHCI builds prove flag OFF/ON compilation, but `/` has no animated-logo consumer, so they do not measure animation cost. Source typography migration and later game hosting remain out of scope.

## Live policy, CI and deployment findings

| ID                                                    | Evidence and impact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Disposition / owner / latest needed                                                                                                                                                                                                                                                                                                                                      |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| FS03-01 — Policy drift, verified                      | Live main protection contains no required status-check or PR-review requirement; linear history is false. No active rulesets/branch rules were returned. Force pushes and deletion are disabled; admin enforcement is false. Saved `.github/branch-protection.json` instead names `ci` and `playwright`, PR review rules and linear history. CONTRIBUTING also claims enforced PR/linear rules. GitHub currently enables squash, rebase and merge commits; auto-delete after merge is false.                                                | Do not run `scripts/protect-main.sh` or silently restore the snapshot. Dimi selects intended policy; Codex proposes exact settings separately before integration. A reviewed PR and successful checks remain the proposed process even where GitHub does not enforce them.                                                                                               |
| FS03-02 — Candidate CI absent, verified               | Candidate has zero GitHub Actions runs, one successful `Vercel Preview Comments` check and successful Vercel status. Workflow triggers only main pushes and PRs into main. Main's [CI run 32488791543](https://github.com/DitmarF/funkspace-app/actions/runs/32488791543) passed on `15c0f1c43a976fb345dbf642ceb7445cf70fcabd` on 2026-08-21; actual job name is `Lint, Types, Unit, E2E, LHCI`, not the snapshot's `ci`/`playwright`. Later main CodeQL/Dependabot jobs include successes/failures and are not replacement application CI. | Fresh Linux candidate/PR and resulting-revision evidence required for A. Codex owns validation before integration acceptance. Do not enable nonexistent required contexts or reuse older main CI as merge evidence.                                                                                                                                                      |
| FS03-03 — Deployment coupling, partly verified        | GitHub reports successful environment **Production** deployment 6022688233 at the exact main SHA and successful **Preview** deployment 6409441779 at the exact candidate SHA. Both payloads have `production_environment: false`; environment labels/statuses do not resolve that discrepancy. GitHub environments have no protection rules. CONTRIBUTING reports automatic main production deployment and branch/PR previews.                                                                                                              | Treat a main merge or branch push as potentially triggering deployment. Dimi confirms actual Vercel production branch, root/build/install commands, outside-root access and preview policy before authorizing a triggering action; Codex verifies resulting build/deployment evidence. Dashboard settings and current production alias remain unverified.                |
| FS03-04 — Unresolved security/provisioning, inherited | FS-0.2's examined graph and audit contain unresolved production and tooling advisories; no dependency graph changed here. Fresh CI uses `pnpm setup:browsers`, while the installed Playwright downloader is an owned FS-0.2 security finding. A successful preview is not a clean security assessment.                                                                                                                                                                                                                                      | Codex/Dimi resolve the bounded downloader remediation or an explicitly reviewed provisioning approach before fresh browser download; review production-relevant findings before deployment. No upgrade, audit-fix or security acceptance is authorized here. Refer to FS-0.2's exact dependency paths/proposals; advisory currency must be rechecked before remediation. |
| FS03-05 — Base/preservation decision pending          | Neither A nor B has Dimi approval. Separate-copy files and other Dimi-only preservation information remain unresolved; the detailed EPIC 0 source is unavailable locally.                                                                                                                                                                                                                                                                                                                                                                   | Dimi owns strategy/name and preservation confirmation; needed for FS-0.3 closure/FS-G0, or before touching any uncertain area. Documentation drafting can continue.                                                                                                                                                                                                      |

The inspected CI generates tokens, checks frontend types, runs coverage/default E2E, root build and LHCI. It does not explicitly build Storybook or run the separate demo E2E, dedicated game typecheck/test and demo build. Root tests overlap game tests and root build builds the game, but do not substitute for all standalone checks. A later validation record must keep those rows separate. Restoring the saved protection file would risk waiting for check contexts the workflow does not publish.

Deployment guidance in [README](../../README.md#deployment) requires frozen workspace installation, frontend deployment with outside-root sources accessible, and workspace-root `pnpm build`: tokens → common types → frontend's game build → Next build. Verify the actual provider applies that order and build-time flags. A merged game package does not itself publish the standalone demo or implement a portfolio play route.

## Two proposed options

**Recommendation: A, conditional on the explicit preflight and approvals below.** Main is already an ancestor, the accepted game and maintenance are retained together, and integration first gives future portfolio work a shared reviewed base. It avoids the later history reconciliation inherent in B. This is a recommendation to prepare reviewed integration, not a claim that merging or deploying is ready now. B is a valid bounded deferral if Dimi chooses to postpone those operations.

| Decision dimension                | A — Integrate accepted game baseline, then branch                                                                                                                                                                                                                                                                                  | B — Defer integration and use the validated game tip                                                                                                                                                                                                                                                                                                                                                                    |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Actual inputs                     | Main `15c0f1c43a976fb345dbf642ceb7445cf70fcabd`; game `7c70984f48d05a8d41baea24384337f58dd2053d`. Recheck both before approval/execution.                                                                                                                                                                                          | Leave main at `15c0f1c43a976fb345dbf642ceb7445cf70fcabd`; proposed fixed portfolio base `7c70984f48d05a8d41baea24384337f58dd2053d`. Do not substitute a moving branch tip.                                                                                                                                                                                                                                              |
| Concrete method and retained work | Propose a reviewed game → main PR and squash integration, matching CONTRIBUTING's default. Retain all 162-path changes and original game/docs refs for provenance; no branch deletion. Record original head → actual resulting main SHA. Other merge methods require Dimi's choice.                                                | Create the future portfolio branch from the exact fixed candidate only after separate branch authorization. Inherit all game/host/tooling/docs changes and the unresolved FS-0.2 findings. Retain original branches and the separate clone.                                                                                                                                                                             |
| Portfolio base / selection rule   | The actual resulting main commit after the reviewed integration and successful resulting-revision validation. No post-integration SHA exists yet. If fixes alter the candidate, capture/review/revalidate that new input first.                                                                                                    | The full candidate SHA above, subject to Dimi's explicit unmerged-base approval and documented existing validation limits. This FS-0.3 draft is a separate uncommitted artifact, not silently part of that base.                                                                                                                                                                                                        |
| Risks/blockers                    | Policy drift, absent candidate Linux CI, vulnerable fresh browser provisioning, production dependency findings, provider configuration/deployment consequences and Dimi decisions. No divergent-main conflict at current inputs; any new main commits reopen review.                                                               | Same inherited defects remain; B does not remediate security or improve CI. Portfolio pushes alone do not trigger this Actions workflow and may trigger Vercel preview. Main and portfolio temporarily have different baselines; later squash integration requires careful portfolio-only reconciliation.                                                                                                               |
| CI/deployment implications        | Authorize PR creation/any required push only after preview/provisioning consequences are understood. Obtain fresh reviewed-input CI; separately authorize integration with any automatic production consequence understood. Validate the actual resulting main revision and provider result before using it as the portfolio base. | Local baseline is qualified FS-0.2 evidence, not remote CI. Before any future publication, resolve relevant security/provider gaps and authorize that publication. Future PR into main will include inherited game work unless integration/reconciliation order is followed. No main deployment is requested by the strategy itself.                                                                                    |
| Later integration dependency      | Game baseline integration and its validation precede portfolio branch creation; later portfolio integration is reviewed separately. Game EPIC 7 remains after the portfolio milestone, not part of this Git operation.                                                                                                             | First integrate/review/validate the game baseline into main. Then reconcile only portfolio changes against that validated main, compare resulting content, and validate again before portfolio integration. Squash/rebase may produce equivalent content without original ancestry; never replay the 81 game commits blindly or treat missing ancestry as loss. The reconciliation method needs separate authorization. |
| Required Dimi approvals           | Select A and base rule; approve exact PR action/head/base; separately approve any commit/push, settings change, dependency update, merge method and deployment-triggering action; approve branch name/create/switch from the validated result.                                                                                     | Select B with the exact base, inherited dependency and later order; approve proposed branch name and any create/switch/commit/push/preview action separately. Later game/portfolio integrations, settings, upgrades and deployments each require their own action-specific authorization.                                                                                                                               |

`feature/funkspace-minimum-usable` fits the existing `feature/<name>` convention and was absent from inspected local/live refs. It is only a proposed name; Dimi has not selected it. No branch cleanup is necessary for either option.

### Decision and execution ledger

| Item                                                             | Proposed                                     | Approved                                        | Executed         | Validated                                            |
| ---------------------------------------------------------------- | -------------------------------------------- | ----------------------------------------------- | ---------------- | ---------------------------------------------------- |
| A and resulting-main base rule                                   | Yes; recommended                             | Pending Dimi                                    | No               | No resulting revision exists                         |
| B and fixed unmerged base                                        | Yes; permitted alternative                   | Pending Dimi                                    | No               | FS-0.2 input mapped to commit; strategy not approved |
| Portfolio branch name/create/switch                              | Name proposed above                          | Pending Dimi                                    | No               | NOT RUN                                              |
| PR, integration, settings, upgrades, deployment, branch deletion | Requirements described; no deletion proposed | None under FS-0.3                               | No               | NOT RUN                                              |
| This decision documentation                                      | Authorized by current assignment             | Drafting authorized; strategy approval separate | Yes, uncommitted | Documentation checks recorded below                  |

## Later authorized continuation

1. Read this record and the actual Dimi decision. Recheck cwd, instructions, full local/live main and game SHAs, upstream, staged/unstaged/untracked work and protected artifacts. If a SHA moved, reassess the delta and obtain approval for changed action inputs; never reset to these recorded SHAs.
2. Resolve owned preflight items relevant to the action, including fresh browser provisioning and automatic preview/production consequences. Keep policy/settings changes distinct from application integration. Do not run the saved protection script as a shortcut.
3. Perform only the specifically authorized operation. Capture actual PR head, tested merge ref where applicable, main input, check names/run IDs and outcome. Keep synthetic PR merge validation separate from final main validation; do not invent a result SHA.
4. For A, validate the actual resulting revision in a clean, deliberately provisioned environment using FS-0.2's verified 15-command order and separate demo E2E. Record every exit, diff/generated change, suppression and skip. Add the existing whole-game ESLint command from the game README when reviewing game changes. Preserve thresholds, contrast filters pending FS-1.2, public boundaries and accepted gameplay. Capture fresh post-integration CI and deployment outcome independently. If anything fails, keep the portfolio base unvalidated and document the owner; do not mask it with older CI.
5. For B, record Dimi's exact approved fixed base and later integration order. A precise approved unmerged-base strategy can close FS-0.3 without a PR, merge or deletion. It does not approve FS-G0 by itself, waive preservation questions or permit publication. After separate branch authorization, verify its actual base and hand off the exact SHA plus any accessible documentation diff to Sites.

## Artifacts and validation performed

Local raw evidence is in `/private/tmp/fs-0.3-evidence/`; it is temporary and is not published CI. The [candidate proof](/private/tmp/fs-0.3-evidence/candidate-proof.json) maps FS-0.2 logs/diffs to the commit. The [integration input diff](/private/tmp/fs-0.3-evidence/main-game.diff), [unique commits](/private/tmp/fs-0.3-evidence/unique-commits.txt), [patch-equivalence output](/private/tmp/fs-0.3-evidence/patch-equivalence.txt), [retained paths](/private/tmp/fs-0.3-evidence/retained-files.txt), timestamped GitHub GET responses and local/protected-copy snapshots preserve the inspection. The [FS-0.2 archive](/private/tmp/fs-0.2-evidence.tar.gz) contains the baseline logs; retain these artifacts with acceptance if another machine needs them.

| Check actually performed in FS-0.3                                                                        | Outcome and limits                                                                                                                    |
| --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| Git status/refs, full SHAs, merge-base, ahead/behind counts, `git cherry`, commit parents and diff review | PASS; main/game match live GitHub. No branch or integration mutation.                                                                 |
| FS-0.2 final patch reconstruction and 16 post-check log/diff comparisons                                  | PASS; exact committed candidate and executable changes match. This checks evidence provenance, not a fresh application run.           |
| GitHub GET metadata inspection                                                                            | PASS after sandbox network access was unavailable on the first attempt; accessible authenticated reads succeeded. No remote mutation. |
| Content, local Markdown targets/anchors, Prettier check, diff whitespace and preservation fingerprints    | PASS; final results in `/private/tmp/fs-0.3-evidence/validation.json`. Only this new task document changes.                           |
| Application/build/browser/CI reruns, new audit, integration and deployment                                | NOT RUN in FS-0.3; documentation-only task. Vercel settings/build logs remain inaccessible/unverified, rather than passing.           |
| Strategy selection and FS-G0                                                                              | BLOCKED on Dimi's decision/preservation confirmation; no human gate approved.                                                         |

## Completion record and handoff

One documentation file was created: `docs/tasks/fs-0.3-branch-integration-decision.md`. Current candidate remains `7c70984f48d05a8d41baea24384337f58dd2053d` plus this untracked document; no contracts changed. The [documentation patch](/private/tmp/fs-0.3-evidence/final.diff) and [evidence archive](/private/tmp/fs-0.3-evidence.tar.gz) are accessible locally. Decision drafting is delivered; task closure and FS-G0 remain pending. No scope deviation or implementation is hidden in the diff.

**Dimi's next acceptance action:** choose A with its resulting-main base rule, or B with the exact fixed SHA and inherited integration order; confirm the proposed branch name and outstanding preservation information. Supplying the detailed local EPIC 0 plan remains pending as previously agreed. Strategy selection alone does not authorize execution of the listed Git, settings, upgrade or deployment actions.

**Sites handoff:** do not infer a selected portfolio base yet. Once Dimi records the strategy and any separately authorized branch exists, verify the full SHA and supplied diff before work. Consume the FS-0.1 reuse map and FS-0.2 environment/coverage limits, preserve the independent game and original assets, and do not independently merge or delete branches. No Dimi visual approval is claimed.
